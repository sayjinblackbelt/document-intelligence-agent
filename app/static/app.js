const form=document.getElementById('upload-form'),status=document.getElementById('status'),results=document.getElementById('results');function fillList(id,values){const e=document.getElementById(id);e.innerHTML='';const items=Array.isArray(values)?values:[];if(!items.length){e.innerHTML='<li>Nenhum indício identificado.</li>';return}items.forEach(v=>{const li=document.createElement('li');li.textContent=v;e.appendChild(li)})}form.addEventListener('submit',async event=>{event.preventDefault();const file=document.getElementById('file').files[0];if(!file)return;status.textContent='Analisando documento...';results.hidden=true;const data=new FormData();data.append('file',file);try{const response=await fetch('/analyze/file',{method:'POST',body:data}),result=await response.json();if(!response.ok)throw new Error(result.detail||'Falha na análise');document.getElementById('type').textContent=result.tipo_documento||'—';document.getElementById('score').textContent=String(result.score_completude??0)+'%';document.getElementById('characters').textContent=result.caracteres??'—';const k=result.palavras_chave||{};fillList('requirements',k.requisitos);fillList('pending',k.pendencias);fillList('risks',k.riscos);document.getElementById('json').textContent=JSON.stringify(result,null,2);results.hidden=false;status.textContent='Análise concluída: '+result.arquivo}catch(error){status.textContent='Erro: '+error.message}});